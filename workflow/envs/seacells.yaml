name: seacells
# using the second method described in https://github.com/dpeerlab/SEACells/tree/main because the first one resulted in import errors
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - bioconda::seacells
  - papermill
  - jupyter